Do *not* add comments to the code you generate.

NEVER run git WRITE commands like `git add` or `git commit`.

You can run READ commands without asking for permission (eg, `ls`, `find`,
`tree` , etc).
